clear;
close all;

elementType = 'P1_1';

% Load mesh
filename = strcat('data/laplace/problemToby/mesh',elementType,'.mat');
load(filename);
clear elementType filename;

% Load settings and solve problem
settings = loadSettings;

[u,error] = laplace(mesh,dof,settings);